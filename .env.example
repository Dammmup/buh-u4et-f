# Absolute URL of the API, including the /api suffix.
# Leave unset to use same-origin /api (when the API is behind the same domain).
# Example: VITE_API_URL=https://api.bukhuchet.kz/api
VITE_API_URL=http://localhost:3000/api
